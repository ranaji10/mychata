# Database schema (generated)

Generated by `bun run docs:gen` from all migrations. Do not edit by hand.

## Functions

| Function | Args | Definer | anon | signed-in |
| --- | --- | --- | --- | --- |
| accept_invitation | _token text | yes |  | yes |
| account_plan | _account_id uuid | yes |  | yes |
| add_property | _name text, _address text, _city text, _rooms integer | yes |  | yes |
| audit_decisions | – | yes | yes | yes |
| bump_usage | _kind text, _daily_limit integer | yes |  | yes |
| claim_initial_membership | – | yes |  | yes |
| create_account_onboarding | _type text, _account_name text, _property_name text, _address text, _city text, _rooms integer, _seasons text[], _overlap_max_guests integer, _house_rules text | yes |  | yes |
| current_account_id | – | yes | yes | yes |
| current_member_id | – | yes | yes | yes |
| feature_enabled | _key text | yes | yes | yes |
| guard_public_form | – | yes | yes | yes |
| has_account_role | _account_id uuid, _role text | yes | yes | yes |
| has_role | _user_id uuid, _role app_role | yes | yes | yes |
| in_current_account | _property_id uuid | yes | yes | yes |
| is_admin | – | yes | yes | yes |
| is_admin_only_file | _name text | yes |  | yes |
| is_member | _account_id uuid | yes | yes | yes |
| is_property_admin | _property_id uuid | yes | yes | yes |
| plan_limit | _account_id uuid, _key text | yes |  | yes |
| public_calendar | _token text | yes | yes | yes |
| public_flag_manual_section | _token text, _section_id uuid | yes | yes | yes |
| public_guest_availability | _token text | yes | yes | yes |
| public_guest_link | _token text | yes | yes | yes |
| public_manual | _token text | yes | yes | yes |
| public_property | _token text | yes | yes | yes |
| rotate_public_token | _property_id uuid | yes |  | yes |
| search_manual | _property_id uuid, _embedding vector, _query text, _lang text, _count integer |  |  | yes |
| set_active_account | _account_id uuid | yes |  | yes |
| set_member_role | _member_id uuid, _role text | yes |  | yes |
| set_public_calendar | _property_id uuid, _enabled boolean | yes |  | yes |
| set_updated_at | – |  | yes | yes |
| submit_guest_request | _token text, _name text, _email text, _start date, _end date, _guests integer, _note text | yes | yes | yes |
| submit_institutional_request | _token text, _name text, _email text, _phone text, _affiliation text, _start date, _end date, _guests integer, _note text | yes | yes | yes |

## Tables

### accounts

`id` uuid · `type` text · `name` text · `created_at` timestamp with time zone

Policies: Members read their accounts (SELECT)

### audit_log

`id` bigint · `account_id` uuid? · `actor_user_id` uuid? · `action` text · `target_type` text? · `target_id` uuid? · `detail` jsonb · `at` timestamp with time zone

Policies: Admins read audit log (SELECT)

### billing_events

`id` bigint · `provider` text · `provider_event_id` text · `type` text · `payload` jsonb · `received_at` timestamp with time zone · `processed_at` timestamp with time zone?

### bookings

`id` uuid · `property_id` uuid · `requester_name` text · `requester_member_id` uuid? · `start_date` date · `end_date` date · `guests` integer · `note` text? · `status` text · `created_at` timestamp with time zone · `updated_at` timestamp with time zone

Policies: Booker or admin deletes booking (DELETE); Booker or admin updates booking (UPDATE); Members create bookings (INSERT); Members read bookings (SELECT)

### consent_log

`id` uuid · `user_id` uuid? · `anonymous_id` text? · `analytics` boolean · `version` text · `created_at` timestamp with time zone

Policies: Users log own or anonymous consent (INSERT)

### documents

`id` uuid · `property_id` uuid · `linked_task_id` uuid? · `title` text · `category` text · `notes` text? · `file_url` text? · `visibility` text · `issue_date` date? · `expiry_date` date? · `created_at` timestamp with time zone · `updated_at` timestamp with time zone

Policies: Admins manage own documents (ALL); Members read own visible documents (SELECT)

### expense_splits

`id` uuid · `expense_id` uuid · `member_id` uuid · `amount_owed` numeric · `paid_back` boolean · `paid_back_confirmed_by` uuid?

Policies: Members add splits (INSERT); Members read splits (SELECT); Receiver or admin confirms split (UPDATE); Receiver or admin deletes split (DELETE)

### expenses

`id` uuid · `property_id` uuid · `amount` numeric · `category` text · `receipt_photo_url` text? · `split_method` text · `paid_by_member_id` uuid? · `created_at` timestamp with time zone · `description` text? · `date` date?

Policies: Members add expenses (INSERT); Members read expenses (SELECT); Payer or admin deletes expense (DELETE); Payer or admin updates expense (UPDATE)

### feature_flags

`key` text · `enabled` boolean · `note` text? · `updated_at` timestamp with time zone

Policies: Anyone reads flags (SELECT)

### guest_links

`id` uuid · `property_id` uuid · `token` text · `created_by_member_id` uuid? · `valid_from` date? · `valid_to` date? · `expires_at` timestamp with time zone? · `revoked_at` timestamp with time zone? · `created_at` timestamp with time zone

Policies: Members manage own account guest links (ALL)

### guest_requests

`id` uuid · `guest_link_id` uuid · `property_id` uuid · `guest_name` text · `guest_email` text? · `guest_phone` text? · `start_date` date · `end_date` date · `guests` integer · `note` text? · `status` text · `created_at` timestamp with time zone

Policies: Admins decide guest requests (UPDATE); Members read own account guest requests (SELECT)

### handover_issues

`id` uuid · `handover_id` uuid? · `property_id` uuid · `task_id` uuid? · `title` text · `acknowledged_at` timestamp with time zone? · `created_at` timestamp with time zone

Policies: Members manage own handover issues (ALL)

### handovers

`id` uuid · `property_id` uuid · `booking_id` uuid? · `member_id` uuid? · `checklist_state` jsonb · `note` text? · `photo_url` text? · `submitted_at` timestamp with time zone · `updated_at` timestamp with time zone

Policies: Author or admin deletes handover (DELETE); Author or admin updates handover (UPDATE); Members read handovers (SELECT); Members submit handovers (INSERT)

### institutional_requests

`id` uuid · `property_id` uuid · `requester_name` text · `requester_email` text · `requester_phone` text? · `affiliation` text? · `start_date` date · `end_date` date · `guests` integer · `note` text? · `status` text · `decline_reason` text? · `has_conflict` boolean · `conflict_note` text? · `created_at` timestamp with time zone

Policies: Members manage own requests (ALL)

### invitations

`id` uuid · `account_id` uuid · `property_id` uuid? · `email` text? · `role` text · `token` text · `status` text · `created_by_member_id` uuid? · `created_at` timestamp with time zone · `expires_at` timestamp with time zone · `accepted_by` uuid?

Policies: Admins create invitations (INSERT); Admins read invitations (SELECT); Admins update invitations (UPDATE)

### manual_chunks

`id` uuid · `property_id` uuid · `section_id` uuid? · `content` text · `embedding` USER-DEFINED? · `created_at` timestamp with time zone · `lang` text · `visibility` text · `content_hash` text? · `model_version` text? · `updated_at` timestamp with time zone · `tsv` tsvector?

Policies: Admins delete chunks (DELETE); Admins insert chunks (INSERT); Admins update chunks (UPDATE); Members read own account chunks (SELECT)

### manual_feedback

`id` uuid · `section_id` uuid · `note` text? · `status` text · `created_at` timestamp with time zone · `resolved_at` timestamp with time zone?

Policies: Members manage own manual feedback (ALL)

### manual_sections

`id` uuid · `property_id` uuid · `category` text · `title_cs` text · `title_en` text · `content_cs` text · `content_en` text · `photo_url` text? · `visibility` text · `display_order` integer · `created_at` timestamp with time zone · `updated_at` timestamp with time zone

Policies: Admins manage own manual sections (ALL); Members read own manual sections (SELECT)

### members

`id` uuid · `account_id` uuid · `name` text · `email` text · `phone` text? · `role` text · `branch` text · `created_at` timestamp with time zone · `user_id` uuid?

Policies: Members read members of their accounts (SELECT); Members update self (UPDATE)

### onboarding_answers

`user_id` uuid · `answers` jsonb · `updated_at` timestamp with time zone

Policies: Users manage own onboarding answers (ALL)

### org_join_requests

`id` uuid · `account_id` uuid · `user_id` uuid · `email` text · `status` text · `created_at` timestamp with time zone

Policies: Admins read join requests (SELECT); Admins update join requests (UPDATE); Users create own join requests (INSERT)

### plans

`id` text · `name` text · `audience` text · `price_czk_month` numeric? · `limits` jsonb · `active` boolean

Policies: Anyone reads plans (SELECT)

### profiles

`user_id` uuid · `member_id` uuid? · `created_at` timestamp with time zone · `display_name` text? · `avatar_url` text? · `phone` text? · `onboarding_completed_at` timestamp with time zone? · `active_account_id` uuid?

Policies: Users insert own profile (INSERT); Users manage own profile (ALL); Users update own profile (UPDATE)

### properties

`id` uuid · `account_id` uuid · `name` text · `address` text · `photo_url` text? · `house_rules_text` text? · `auto_confirm` boolean · `handover_items` jsonb · `created_at` timestamp with time zone · `rooms` integer? · `city` text? · `overlap_max_guests` integer? · `peak_seasons` ARRAY · `created_by_member_id` uuid? · `public_token` text · `public_calendar_enabled` boolean

Policies: Members read own properties (SELECT)

### property_admins

`id` uuid · `property_id` uuid · `member_id` uuid

Policies: Admins manage property admins (INSERT); Admins remove property admins (DELETE); Members read own account property admins (SELECT)

### property_photos

`id` uuid · `property_id` uuid · `storage_path` text · `uploaded_by_member_id` uuid? · `is_primary` boolean · `created_at` timestamp with time zone

Policies: Members read photos (SELECT); Members set main photo (UPDATE); Members upload photos (INSERT); Uploader or admin deletes photo (DELETE)

### subscriptions

`account_id` uuid · `plan_id` text · `status` text · `seats` integer? · `current_period_end` timestamp with time zone? · `billing_email` text? · `company_id` text? · `vat_id` text? · `provider` text? · `provider_ref` text? · `updated_at` timestamp with time zone

Policies: Admins read own subscription (SELECT)

### tasks

`id` uuid · `property_id` uuid · `title` text · `category` text · `photo_url` text? · `urgency` text · `assignee_member_id` uuid? · `status` text · `due_date` date? · `done_note` text? · `created_by` text · `created_at` timestamp with time zone · `source_language` text · `title_cs` text? · `title_en` text? · `description_cs` text? · `description_en` text? · `updated_at` timestamp with time zone

Policies: Admins delete tasks (DELETE); Members create tasks (INSERT); Members read tasks (SELECT); Members update tasks (UPDATE)

### usage_counters

`user_id` uuid · `kind` text · `day` date · `count` integer

Policies: Users read own usage (SELECT)

### user_roles

`id` uuid · `user_id` uuid · `role` USER-DEFINED

Policies: Users read own roles (SELECT)
