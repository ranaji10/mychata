# T-004 Signed-in end-to-end tests

Status: todo, after T-003
Files allowed: tests/e2e/**, playwright.config.ts
Goal: Playwright covers onboarding, invite an existing user, switch account, guest request, make/remove admin, settle an expense (receiver only), public calendar publish.
Acceptance: all pass on staging in CI (`workflow_dispatch` with the staging URL).
